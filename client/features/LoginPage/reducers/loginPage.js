import * as loginPageActions from '../actions/ActionTypes';

export default function loginPage(state = initState, action) {
  switch (action.type) {
    case loginPageActions.LOGIN_STARTED:
      
  }
}